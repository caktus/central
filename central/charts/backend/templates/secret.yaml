apiVersion: v1
kind: Secret
metadata:
  name: {{ include "backend.fullname" . }}
  labels:
    {{- include "backend.labels" . | nindent 4 }}
{{- with .Values.central.env }}
stringData:
  {{- toYaml . | nindent 2 }}
{{- end }}
type: Opaque
