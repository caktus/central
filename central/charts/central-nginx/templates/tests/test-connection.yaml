apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "central-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "central-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "central-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
