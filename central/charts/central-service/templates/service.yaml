apiVersion: v1
kind: Service
metadata:
  name: {{ include "central-service.fullname" . }}
  labels:
    {{- include "central-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "central-service.selectorLabels" . | nindent 4 }}
