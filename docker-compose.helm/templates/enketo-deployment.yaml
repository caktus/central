apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f docker-compose.helm.yml -c
    kompose.version: 1.31.2 (HEAD)
  creationTimestamp: null
  labels:
    io.kompose.service: enketo
  name: enketo
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: enketo
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f docker-compose.helm.yml -c
        kompose.version: 1.31.2 (HEAD)
      creationTimestamp: null
      labels:
        io.kompose.network/central-default: "true"
        io.kompose.service: enketo
    spec:
      containers:
        - env:
            - name: DOMAIN
            - name: HTTPS_PORT
              value: "443"
            - name: SUPPORT_EMAIL
          image: enketo
          name: enketo
          ports:
            - containerPort: 8005
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /etc/secrets
              name: secrets
      restartPolicy: Always
      volumes:
        - name: secrets
          persistentVolumeClaim:
            claimName: secrets
status: {}
